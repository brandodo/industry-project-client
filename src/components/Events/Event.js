import React, { useEffect, useState } from "react";

export default function Event({ item }) {
  return (
    <div className="events__eventCard">
      {/* <img src={item.thumbnailUrl} alt="event" /> */}
      Event
    </div>
  );
}
